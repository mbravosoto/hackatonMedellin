import { Component, OnInit } from '@angular/core';

@Component({
    standalone: true,
    // eslint-disable-next-line @angular-eslint/component-selector
    selector: 'bob-test',
    template: `
    jijijija
    `
})

// eslint-disable-next-line @angular-eslint/component-class-suffix
export class BobTest {
}